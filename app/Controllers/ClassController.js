const user = require('../Models/User');
const classe = require('../Models/Class');

const ClassController = {};

ClassController.createClass = async (req, res)=>{
    try{
        
    }catch{

    }
};

module.export = ClassController